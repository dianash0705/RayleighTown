[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    unit: fast, isolated unit tests (default)
    integration: tests that exercise multiple modules together
    noise: tests that use synthetic noise scenarios
    slow: longer-running scenarios (large event counts, many periods)
